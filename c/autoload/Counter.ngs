{
	doc CANDIDATE FOR REMOVAL in favor of more generic Iter(r:Range) which is not written yet.
	type Counter
}

doc Counter constructor. Sets the counter to 0.
F init(c:Counter) c.val = 0

doc Get next counter value.
doc %EX - c = Counter(); [c.next(), c.next()]  # [0, 1]
F next(c:Counter) {
	ret = c.val
	c.val += 1
	ret
}

TEST c=Counter(); [c.next(), c.next()] == [0, 1]
