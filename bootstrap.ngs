def bootstrap_require(f) {
	content = fetch_file(f)
	code = compile(content, f)
	lambda_ = load(code)
	lambda_()
}

def bootstrap() {
	  bootstrap_require(NGS_FOLDER + '/' + 'stdlib.ngs')
	  require(ARGV[2])
}

bootstrap()

